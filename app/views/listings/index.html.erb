<div class="jumbotron" alt="Credits to Tom Hilton for the picture - Flickr URL: https://www.flickr.com/photos/tomhilton/">
  <h1 class="justify center">Secondhand Objects, Art, Clothes and Furniture from all around the world.</h1><br>
</div>
<h2 class="justify">A community where people can buy and sell cool and stylish secondhand objects, art, clothes and furniture. </h2>
<div class="center">
  <div class="row">
    <% @listings.each do |listing| %>
      <div class="col-md-3">
              <div class="thumbnail">
                <%= link_to image_tag(listing.image.url), listing %>
                  <div class="caption">
                    <h3><%= listing.name %></h3>
                    <p><%= number_to_currency(listing.price) %></p>
                    <p><%= "Sold by #{listing.user.name}" %></p>
                    <p><%= listing.description %></p>
                  </div>
                </div>
          </div>
    <% end %> 
  </div>
</div>

<br>

<% if user_signed_in? %>
<%= link_to "New Listing", new_listing_path, class: "btn btn-link" %>
<% end %>